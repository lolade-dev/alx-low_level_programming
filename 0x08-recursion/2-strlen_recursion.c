#include "main.h"
/**
 * _strlen_recursion - Entry point
 * @s: input
 * Return: the length of the string
 */
int _strlen_recursion(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; ++i)
	{
	}

	return (i);
}
